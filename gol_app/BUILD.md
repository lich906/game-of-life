## Build project using Visual Studio

Before building project install dependenices using vcpkg:
```ps
vcpkg install
```

After that you are ready to Build gol_app project via Visual Studio (tested with Visual Studio 2022)
